<?xml version="1.0" encoding="UTF-8"?>
<knimeNode icon="./googledriveconnector-icon.png"
		type="Source"
		xmlns="http://knime.org/node/v4.1"
		xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		xsi:schemaLocation="http://knime.org/node/v4.1 http://knime.org/node/v4.1.xsd">
    <name>Google Drive Connector (Labs)</name>
    
    <shortDescription>
        Connects to Google Drive file system by Google API in order to read/write files in downstream nodes.
    </shortDescription>
    
    <fullDescription>
         <intro>
            <p>This node connects to remote Google Drive file system. The resulting output port allows downstream nodes to access
            the <i>files</i> of the remote file system, e.g. to read or write,
            or to perform other file system operations (browse/list files, copy, move, ...).
            </p>
            
            <p><b>Path syntax:</b> Paths for Google Drive are specified with a UNIX-like syntax, /myfolder/myfile. An absolute
            for Google Drive consists of:
                <ol>
                    <li>A leading slash ("/").</li>
                    <li>Followed by the path to the file ("myfolder/myfile" in the above example).</li>
                </ol>
            </p>
        </intro>

        <tab name="Settings">
		    <option name="Working directory">
		        Specify the <i>working directory</i> of the resulting file system connection, using the <b>Path syntax</b> explained above. The working
		        directory must be specified as an absolute path. A working directory allows downstream nodes to access files/folders using <i>relative</i>
		        paths, i.e. paths that do not have a leading slash. The default working directory is the root "/".
		    </option>
	    </tab>
	    
        <tab name="Advanced">
		    <option name="Connection timeout">
		        Timeout in seconds to establish a connection or 0 for an infinite timeout. 
		    </option>
		    <option name="Read timeout">
		        Timeout in seconds to read data from connection or 0 for an infinite timeout. 
		    </option>
		</tab>
    </fullDescription>

    <ports>
    	<inPort name="Google Api Connection" index="0">
    		Configured Google API Connection.
    	</inPort>
   		<outPort index="0" name="Google Drive File System Connection">
   			Google Drive File System Connection.
   		</outPort>
    </ports>    
</knimeNode>
